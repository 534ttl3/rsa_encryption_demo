#ifndef TOOLS_H
#define TOOLS_H

#include <string>
#include <stdlib.h>

unsigned int convertBinaryToDecimal(std::string& binstr);
int convertToBinaryNotation(unsigned long decnum, std::string& binarystr);

#endif // TOOLS_H
